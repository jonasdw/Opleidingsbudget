{% extends '::base.html.twig' %}

{% block body %}
{% for user in users %}
    {% if not user.isEnabled %}
        <a href="{{ path('tactics_edit_user', { 'username': user.username }) }}">{{ user.username|e }} </a> | disabled<br />
    {#{% for role in user.roles %}
        {{ role|slice(5) }}
        <input type="checkbox" checked="checked" value="role" placeholder="" name="user.username">
    {% endfor %}#}
    {% else %}
        <a href="{{ path('tactics_edit_user', { 'username': user.username }) }}">{{ user.username|e }}</a><br />
    {% endif %}
{% endfor %}
{% endblock %}