{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ExpenseRequest creation</h1>

    {{ form(form) }}

<ul class="record_actions">
    <li>
        <a href="{{ path('expenserequest') }}">
            Back to all requests
        </a>
    </li>
    <li>
        <a href="{{ path('transaction') }}">
            Back to transactions
        </a>
    </li>
</ul>
{% endblock %}
